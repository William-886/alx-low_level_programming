#include "main.h"

/**
 * _abs - function name
 * Description: computes absolute value of an integer
 * Return: absolute value of integer
 */

int _abs(int r)
{
    if (r >= 0)
    {
        return (r);
    }
    else
    {
        return (r * (-1));
    }
}
