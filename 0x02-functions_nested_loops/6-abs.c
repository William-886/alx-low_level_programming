#include "main.h"

/**
 * _abs - function that computes absolute value of an integer
 * @r: parameter of type int 
 * Return: absolute value of integer
 */

int _abs(int r)
{
	if (r >= 0)
	{
	return (r);
	}
		else
		{	
		return (r * (-1));
		}
}
