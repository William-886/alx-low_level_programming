#include <stdio.h>

/**
 * Main - Entry point
 * Description: Print single digit numbers followed by a new line
 * Return: Always 0
 */

int main(void)
{
int a;
for (a = 48; a <= 57; a++)
{
putchar (a);
}
putchar ('\n');
return (0);
}
